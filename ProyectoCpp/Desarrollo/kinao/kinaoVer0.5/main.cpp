#include "cpp_global.h"
#include "skel_init.h"


int main(){
/*
    using namespace std;


    float a[3]={1, 0, 0};
    float b[3]={-1, -10, 0};
    float c[3]={0, 0, 0};
    float d, e, f, g, h, i;
    Joint result;

    d=result.length(a,b);///opuesto
    e=result.length(b,c);
    f=result.length(c,a);
    g=result.angle(e, f, d);
    h=result.angle(d, f, e);
    i=result.angle(e, d, f);
   
	cout << g << endl;
	cout << h << endl;
	cout << i << endl;
	//cout << pow(e, 2) << endl;
	//cout << pow(d, 2) << endl;
	

    return (0);
*/

 
}
